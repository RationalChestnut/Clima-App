import colors from "./colors.theme";
import sizes from "./sizes.theme";
import { fonts, fontWeights, fontSizes } from "./fonts.theme";

const theme = {
  colors,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};

export default theme;
